# Gateway Server Configuration

# URL del servidor de configuración (requerido)
CONFIG_API_URL=http://localhost:8000

# API Key para acceder al KV Storage (requerido)
API_KEY=test-token-123

# Clave para desencriptar tokens de backends
ENCRYPTION_KEY=go-oracle-api-secure-key-2026

# Puerto local (solo para ejecución local, ignorado en Deno Deploy)
PROXY_PORT=8080

# TTL del caché de backends en milisegundos (default: 30000)
CACHE_TTL_MS=30000
