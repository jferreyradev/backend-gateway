# Gateway Server Configuration

# Puerto del gateway (ignorado en Deno Deploy)
PORT=8080

# URL de la API de almacenamiento - Puede ser:
# - Deno KV Storage API
# - Tu propia API REST compatible
# - Cualquier sistema que soporte GET/POST/DELETE con estructura KV
STORAGE_URL=https://kv-storage-api.deno.dev

# API Key para acceder (requerido)
API_KEY=tu-api-key

# Clave para encriptar/desencriptar tokens de backends (requerido, mínimo 32 caracteres)
# Los tokens de los backends se guardan ENCRIPTADOS en el KV Storage usando AES-GCM 256-bit
# El gateway desencripta estos tokens para enviarlos a las APIs backend
# DEBE SER LA MISMA en todas las PCs que registran backends y en el gateway
ENCRYPTION_KEY=go-oracle-api-secure-key-2026

# TTL del caché de backends en milisegundos (default: 30000)
CACHE_TTL_MS=30000

# TTL de tokens de autenticación en milisegundos (default: 3600000 = 1 hora)
TOKEN_TTL_MS=3600000

# Orígenes CORS permitidos, separados por comas
# Usa '*' para permitir todos (solo desarrollo)
# En producción, especifica los dominios exactos
ALLOWED_ORIGINS=*
# ALLOWED_ORIGINS=https://myapp.com,https://admin.myapp.com

# Credenciales de acceso al gateway (legacy, ya no se usa)
# GATEWAY_USERNAME=admin
# GATEWAY_PASSWORD=admin123
