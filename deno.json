{
  "tasks": {
    "dev": "deno serve --allow-net --allow-env src/simple-gateway.ts",
    "start": "deno serve --allow-net --allow-env src/simple-gateway.ts",
    "register": "deno run -A scripts/register-backend.ts",
    "register:simple": "deno run -A scripts/register-backend-simple.ts",
    "register:bulk": "deno run -A scripts/register-backends-bulk.ts",
    "register:user": "deno run -A scripts/register-user.ts",
    "delete": "deno run -A scripts/delete-backend.ts",
    "check": "deno run -A scripts/check-backends.ts",
    "test": "deno run -A scripts/test-general.ts",
    "test:auth": "deno run -A scripts/test-auth.ts",
    "test:gateway": "deno run -A scripts/test-gateway.ts"
  },
  "fmt": {
    "indentWidth": 4,
    "lineWidth": 100
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
